<a href="{{ $link }}" class="header-image-button" target=”_blank”>
    <img
        class="header-image-button-image"
        src="{{ asset('img/' . $imageName . '.svg') }}"
        alt="{{ $alt }}"
        title="{{ $tooltip }}"
    >
</a>
